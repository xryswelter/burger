<h1>Eat Dat Hamburger!</h1>

<h2>Burgers Ready to Cosume</h2>

<ul>
  {{#each hamburger}}
    {{#unless consumption}}
      {{> burger/burger-partial consumption=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Hamburgers You Might Like to Have Made Again</h2>

<ul>
  {{#each hamburger}}
    {{#if consumption}}
      {{> burger/burger-partial consumption=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a New Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="br">Burger Name</label>
    <input type="text" id="br" name="name">
  </div>

  <button type="submit">Enter In Hamburger</button>
</form>
